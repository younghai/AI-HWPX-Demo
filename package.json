{
  "name": "ai-hwp",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"PORT=8792 pnpm -C server dev\" \"pnpm -C client dev\"",
    "dev:auto": "concurrently \"PORT=8793 CLIENT_ORIGIN=http://127.0.0.1:5193 OAUTH_REDIRECT_BASE=http://127.0.0.1:8793 pnpm -C server dev\" \"VITE_SERVER_PORT=8793 VITE_AUTO_LOGIN=true pnpm -C client dev -- --port 5193\"",
    "dev:client": "pnpm -C client dev",
    "dev:server": "pnpm -C server dev",
    "build": "pnpm -C client build",
    "lint": "pnpm -C client lint",
    "test": "pnpm -C client test",
    "test:all": "pnpm -C client test && pnpm -C server test && ./.venv/bin/python3 -m pytest scripts/tests -q",
    "postinstall": "bash scripts/setup-rhwp-symlink.sh"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
